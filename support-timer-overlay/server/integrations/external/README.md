# External Integrations

Future integrations should live here and emit the same normalized support event shape:

```json
{
  "supporterName": "name",
  "amount": 1000,
  "message": "optional message"
}
```
